#include "main.h"
/**
 * string_length - Returns the length of a string.
 * @str: The input string.
 *
 * Return: The length of the string.
 */

int _strlen(char *s)
{
	int len;

	for (len = 0;len <= s; le++)
	{
		len++;
	}

	return len;
}
